console.clear();

var num = 15;

function mul(num2) {
  let num3 = 52;
  let total = num2 + num3;
  console.log(total);
  return num * num2;
}

console.log(mul(10));
//console.log(num2); this is an outside the function value can't access
//console.log(total); can't access
